/* AUTO-GENERATED by deepspan-codegen — DO NOT EDIT
 * Source: hwip.yaml  hwip={{ desc.name }} v={{ desc.version }}
 * SPDX-License-Identifier: Apache-2.0
 *
 * Zephyr firmware dispatch header for {{ desc.namespace }} HWIP.
 */
#ifndef {{ desc.namespace | upper }}_DISPATCH_H
#define {{ desc.namespace | upper }}_DISPATCH_H

#include <zephyr/sys/util.h>
#include "deepspan_{{ desc.name }}.h"

/* ── Opcode dispatch table ──────────────────────────────────────────────── */

/** Return true if @p op is a recognized {{ desc.name }} opcode. */
static inline bool {{ desc.namespace }}_is_valid_op(uint32_t op)
{
    switch (op) {
{% for opdef in desc.operations %}
    case {{ desc.namespace | upper }}_OP_{{ opdef.name_upper }}:
{% endfor %}
        return true;
    default:
        return false;
    }
}

/**
 * Dispatch a {{ desc.name }} command.
 *
 * @param dev   Zephyr device pointer
 * @param op    Opcode (one of {{ desc.namespace | upper }}_OP_*)
 * @param arg0  First argument (little-endian uint32)
 * @param arg1  Second argument (little-endian uint32)
 * @return 0 on success, negative errno on error
 */
int {{ desc.namespace }}_dispatch(const struct device *dev,
                                   uint32_t op, uint32_t arg0, uint32_t arg1);

#endif /* {{ desc.namespace | upper }}_DISPATCH_H */

/* AUTO-GENERATED by deepspan-codegen — DO NOT EDIT
 * Source: hwip.yaml  hwip=accel v=1.0.0
 * SPDX-License-Identifier: Apache-2.0
 *
 * Zephyr firmware dispatch header for deepspan_accel HWIP.
 */
#ifndef DEEPSPAN_ACCEL_DISPATCH_H
#define DEEPSPAN_ACCEL_DISPATCH_H

#include <zephyr/sys/util.h>
#include "deepspan_accel.h"

/* ── Opcode dispatch table ──────────────────────────────────────────────── */

/** Return true if @p op is a recognized accel opcode. */
static inline bool deepspan_accel_is_valid_op(uint32_t op)
{
    switch (op) {
    case DEEPSPAN_ACCEL_OP_ECHO:
    case DEEPSPAN_ACCEL_OP_PROCESS:
    case DEEPSPAN_ACCEL_OP_STATUS:
        return true;
    default:
        return false;
    }
}

/**
 * Dispatch a accel command.
 *
 * @param dev   Zephyr device pointer
 * @param op    Opcode (one of DEEPSPAN_ACCEL_OP_*)
 * @param arg0  First argument (little-endian uint32)
 * @param arg1  Second argument (little-endian uint32)
 * @return 0 on success, negative errno on error
 */
int deepspan_accel_dispatch(const struct device *dev,
                                   uint32_t op, uint32_t arg0, uint32_t arg1);

#endif /* DEEPSPAN_ACCEL_DISPATCH_H */
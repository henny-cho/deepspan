version: v2

modules:
  - path: proto
    name: buf.build/myorg/deepspan-accel

deps:
  - buf.build/googleapis/googleapis
  # Platform protos (management, telemetry) — for cross-service references
  - buf.build/myorg/deepspan

lint:
  use:
    - DEFAULT
  except:
    - FIELD_NOT_REQUIRED
    - PACKAGE_NO_IMPORT_CYCLE

breaking:
  use:
    - FILE

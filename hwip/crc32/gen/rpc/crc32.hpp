// AUTO-GENERATED by deepspan-codegen — DO NOT EDIT
// Source: hwip.yaml  hwip=crc32 v=1.0.0
// SPDX-License-Identifier: Apache-2.0
#pragma once
#include <cstdint>
#include <utility>

namespace deepspan_crc32 {

// ── HW opcode constants ──────────────────────────────────────────────────────
enum class Op : uint32_t {
    COMPUTE = 0x0001U,  ///< Compute CRC32 over byte stream (IEEE 802.3 poly 0xEDB88320)
    GET_POLY = 0x0002U,  ///< Return current CRC polynomial (default: 0xEDB88320 = IEEE 802.3)
};

// ── Register offset constants ────────────────────────────────────────────────
enum class Reg : uint32_t {
    CTRL = 0x0000U,  ///< Control register
    STATUS = 0x0004U,  ///< Status register
    IRQ_STATUS = 0x0008U,  ///< IRQ status (write-1-to-clear)
    IRQ_ENABLE = 0x000CU,  ///< IRQ enable mask
    VERSION = 0x0010U,  ///< Hardware version register (major<<16 | minor)
    CAPABILITIES = 0x0014U,  ///< Hardware capability flags
    CMD_OPCODE = 0x0100U,  ///< Command opcode register
    CMD_ARG0 = 0x0104U,  ///< compute: data length in bytes; get_poly: unused
    CMD_ARG1 = 0x0108U,  ///< Reserved
    CMD_FLAGS = 0x010CU,  ///< Command flags
    RESULT_STATUS = 0x0110U,  ///< Result status code
    RESULT_DATA0 = 0x0114U,  ///< CRC32 checksum (compute) or polynomial value (get_poly)
    RESULT_DATA1 = 0x0118U,  ///< Reserved
};

// ── Proto enum → HW opcode mapping ──────────────────────────────────────────
// ProtoOpToHwOp converts a proto enum value (1-based) to the hardware wire
// opcode.  Returns {opcode, true} on success; {0, false} for unknown values.
[[nodiscard]] inline std::pair<uint32_t, bool>
proto_op_to_hw_op(int32_t proto_op) noexcept {
    switch (proto_op) {
    case 1: return {static_cast<uint32_t>(Op::COMPUTE), true};
    case 2: return {static_cast<uint32_t>(Op::GET_POLY), true};
    default: return {0u, false};
    }
}

// ── Opcode validation ────────────────────────────────────────────────────────
[[nodiscard]] inline bool validate_opcode(uint32_t op) noexcept {
    switch (static_cast<Op>(op)) {
    case Op::COMPUTE: case Op::GET_POLY:
        return true;
    default:
        return false;
    }
}

}  // namespace deepspan_crc32
/* AUTO-GENERATED by deepspan-codegen — DO NOT EDIT
 * Source: hwip.yaml  hwip=crc32 v=1.0.0
 * SPDX-License-Identifier: Apache-2.0
 *
 * Zephyr firmware dispatch header for deepspan_crc32 HWIP.
 */
#ifndef DEEPSPAN_CRC32_DISPATCH_H
#define DEEPSPAN_CRC32_DISPATCH_H

#include <zephyr/sys/util.h>
#include "deepspan_crc32.h"

/* ── Opcode dispatch table ──────────────────────────────────────────────── */

/** Return true if @p op is a recognized crc32 opcode. */
static inline bool deepspan_crc32_is_valid_op(uint32_t op)
{
    switch (op) {
    case DEEPSPAN_CRC32_OP_COMPUTE:
    case DEEPSPAN_CRC32_OP_GET_POLY:
        return true;
    default:
        return false;
    }
}

/**
 * Dispatch a crc32 command.
 *
 * @param dev   Zephyr device pointer
 * @param op    Opcode (one of DEEPSPAN_CRC32_OP_*)
 * @param arg0  First argument (little-endian uint32)
 * @param arg1  Second argument (little-endian uint32)
 * @return 0 on success, negative errno on error
 */
int deepspan_crc32_dispatch(const struct device *dev,
                                   uint32_t op, uint32_t arg0, uint32_t arg1);

#endif /* DEEPSPAN_CRC32_DISPATCH_H */
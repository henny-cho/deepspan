# SPDX-License-Identifier: Apache-2.0
# buf.gen.yaml — retained for proto lint validation only.
#
# C++ gRPC stubs are generated at CMake build time via protobuf_generate().
# Python stubs are generated via grpc_tools.protoc at SDK install time.
# Neither is committed to the repository.
#
# This file is intentionally minimal; run `buf lint` for proto validation.
version: v2
